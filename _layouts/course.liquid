---
layout: page
---
<div class="course">
  {% if page.instructor or page.term or page.location or page.time %}
    <div class="course-info">
      {% if page.instructor %}
        <p><strong>Profesor:</strong> {{ page.instructor }}</p>
      {% endif %}

      {% if page.term %}
        <p><strong>Período:</strong> {{ page.term }}</p>
      {% endif %}

      {% if page.location %}
        <p><strong>Lugar:</strong> {{ page.location }}</p>
      {% endif %}

      {% if page.time %}
        <p><strong>Horario:</strong> {{ page.time }}</p>
      {% endif %}
    </div>
  {% endif %}

  {{ content }}

  {% if page.schedule %}
    <h2>Programación</h2>
    <table class="table table-sm table-responsive">
      <thead>
        <tr>
          <th>Semana</th>
          <th>Fecha</th>
          <th>Tema</th>
          <th>Materiales</th>
        </tr>
      </thead>
      <tbody>
        {% for entry in page.schedule %}
          <tr>
            <td>{{ entry.week }}</td>
            <td>{{ entry.date }}</td>
            <td>
              {% if entry.topic %}
                <strong>{{ entry.topic }}</strong>
              {% endif %}
              {% if entry.description %}
                <div class="schedule-description">{{ entry.description | markdownify }}</div>
              {% endif %}
            </td>
            <td>
              {% if entry.materials %}
                <ul class="schedule-materials">
                  {% for material in entry.materials %}
                    <li>
                      {% if material.url %}
                        {% if material.url contains '://' %}
                          <a href="{{ material.url }}" target="_blank">{{ material.name }}</a>
                        {% else %}
                          <a href="{{ material.url | relative_url }}" target="_blank">{{ material.name }}</a>
                        {% endif %}
                      {% else %}
                        {{ material.name }}
                      {% endif %}
                    </li>
                  {% endfor %}
                </ul>
              {% endif %}
            </td>
          </tr>
        {% endfor %}
      </tbody>
    </table>
  {% endif %}
</div>
